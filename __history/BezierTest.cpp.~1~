//---------------------------------------------------------------------------

#include <vcl.h>
#pragma hdrstop

#include "BezierTest.h"
//---------------------------------------------------------------------------
#pragma package(smart_init)
#pragma resource "*.dfm"
TForm1 *Form1;
//---------------------------------------------------------------------------
__fastcall TForm1::TForm1(TComponent* Owner)
	: TForm(Owner)
{
}
//---------------------------------------------------------------------------
void __fastcall TForm1::Button1Click(TObject *Sender)
{
	int X1, X2, X3, Y1, Y2, Y3; //координаты заданных пользователем точек
	float Ax, Ay, Bx, By, Px, Py; //координаты вспомогательных точек

	int firstLenght = 1, secondLenght = 999;
	float lyambda; firstLenght / secondLenght;

	TryStrToInt(Edit1->Text, X1);               //передаем координаты от пользователя
	TryStrToInt(Edit2->Text, Y1);
	TryStrToInt(Edit3->Text, X2);
	TryStrToInt(Edit4->Text, Y2);
	TryStrToInt(Edit5->Text, X3);
	TryStrToInt(Edit6->Text, Y3);

	Image1->Picture->Bitmap = NULL;

	Image1->Canvas->Ellipse(X1-2,Y1-2,X1+2,Y1+2);  //рисуем заданные точки
	Image1->Canvas->Ellipse(X2-2,Y2-2,X2+2,Y2+2);
	Image1->Canvas->Ellipse(X3-2,Y3-2,X3+2,Y3+2);

	Image1->Canvas->MoveTo(X1, Y1); //помещаем перо к первой точке
	//Image1->Canvas->LineTo(X3, Y3);

	while(firstLenght<999)
	{
		lyambda = (float)firstLenght / secondLenght;

		Ax = (X1+lyambda*X3)/(1+lyambda);       //первая вспомогательная точка
		Ay = (Y1 + lyambda*Y3)/(1+lyambda);

		Bx = (X3+lyambda*X2)/(1+lyambda);       //вторая вспомогательная точка
		By = (Y3 + lyambda*Y2)/(1+lyambda);

		Px = (Ax+lyambda*Bx)/(1+lyambda);       //третья вспомогательная точка
		Py = (Ay + lyambda*By)/(1+lyambda);

		Form1->Image1->Canvas->LineTo(Px, Py);

		firstLenght++;
		secondLenght--;

	}


}
//---------------------------------------------------------------------------

//---------------------------------------------------------------------------
